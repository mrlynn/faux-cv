{
  "name": "faux-cv",
  "version": "1.6.0",
  "description": "Generate realistic fake resumes in markdown, JSON, and PDF formats",
  "main": "lib/index.js",
  "bin": {
    "faux-cv": "bin/cli.js"
  },
  "scripts": {
    "test": "NODE_ENV=test jest --verbose --setupFilesAfterEnv=./jest.setup.js",
    "test:watch": "NODE_ENV=test jest --watch --setupFilesAfterEnv=./jest.setup.js",
    "test:coverage": "NODE_ENV=test jest --coverage --setupFilesAfterEnv=./jest.setup.js",
    "test:ci": "NODE_ENV=test jest --ci --coverage --setupFilesAfterEnv=./jest.setup.js",
    "test:utils": "NODE_ENV=test jest __tests__/utils.test.js --setupFilesAfterEnv=./jest.setup.js",
    "test:generators": "NODE_ENV=test jest __tests__/generators.test.js --setupFilesAfterEnv=./jest.setup.js",
    "test:core": "NODE_ENV=test jest __tests__/core.test.js --setupFilesAfterEnv=./jest.setup.js",
    "test:pdf": "NODE_ENV=test jest __tests__/pdf.test.js --setupFilesAfterEnv=./jest.setup.js",
    "test:integration": "NODE_ENV=test jest __tests__/integration.test.js --setupFilesAfterEnv=./jest.setup.js",
    "test:cli": "NODE_ENV=test jest __tests__/cli.test.js --setupFilesAfterEnv=./jest.setup.js"
  },
  "keywords": [
    "resume",
    "cv",
    "generator",
    "fake",
    "mock",
    "sample",
    "markdown",
    "json",
    "pdf"
  ],
  "author": "Michael Lynn",
  "license": "MIT",
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "chalk": "^4.1.2",
    "commander": "^11.0.0",
    "mustache": "^4.2.0",
    "Puppeteer": "npm:puppeteer@^22.15.0"
  },
  "optionalDependencies": {
    "puppeteer": "^22.0.0",
    "showdown": "^2.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mock-fs": "^5.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
